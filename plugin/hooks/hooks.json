{
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Workflow reminder: start from intent (the why), check for ready-for-agent tickets before picking up work, cite ADRs for decisions, and paste verification proof before closing anything. Full process: engineering-workflow skill.'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "comment": "Claude Code only — pi uses a different hook mechanism and ignores this entry. Advisory: fires after a Bash tool call (issue close / merge run there) and only reminds; it exits 0 and never blocks.",
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Advisory: before you close an issue or merge, paste the tests/validate.sh run (exit 0, ticket range) beside the acceptance-criteria proof. This is a reminder, not a gate.'; exit 0"
          }
        ]
      }
    ]
  }
}
